# === OPTION 1: Token Manager (Recommended) ===
# Token Manager automatically fetches Access Token from REST API
# Email: transport-app@yourdomain.com
# Application: your-app-name
# Enable in config.yaml: token_manager.enabled = true
# 
# ⚠️ SECURITY WARNING: DO NOT commit real password to git!
# 
# PRODUCTION: Use Jenkins Credentials (ID: webapp-transport-app)
# DEVELOPMENT: Copy this file to .env and replace with real password
RPA_BOT_PASSWORD=your_password_here

# === OPTION 2: Manual SharePoint Token (Fallback) ===
# If Token Manager is disabled or fails, use this manual token
# ⚠️ NIE COMMITUJ TEGO PLIKU DO GIT!
# Skopiuj ten plik jako .env i wklej swój token z Azure
SHAREPOINT_ACCESS_TOKEN=your_access_token_here

# === Email Configuration ===
# Email sending uses MS Graph API with the same access token as SharePoint
# No separate password needed! Token must have Mail.Send permission.
# To enable: Ensure token_manager or SHAREPOINT_ACCESS_TOKEN has Mail.Send scope

